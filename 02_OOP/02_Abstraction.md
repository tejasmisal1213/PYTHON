# Abstraction in Python

## What is Abstraction?

- Abstraction is the process of hiding implementation details and showing only the neccesary information.

## Abstract Class

An abstract class is a class that contains one or more abstract methods.

Python provides the `ABC` class and `abstractmethod` decorator for abstraction.

- from abc import ABC, abstractmethod
 

