# Recursion in Python
- Recursion is a process in which a function calls itself.

- A recursive function must have a base condition to stop the recursion.


## Important Points

- A function can call itself using recursion.
- A base condition is required.
- Without a base condition, recursion can continue indefinitely.
- Recursion is commonly used in DSA and problem solving.